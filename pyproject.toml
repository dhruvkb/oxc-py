[project]
name = "oxc-py"
version = "0.0.0"
description = "Oxfmt and Oxlint distributed through PyPI"
readme = "README.md"
requires-python = ">=3.14"

[dependency-groups]
dev = ["packaging>=26.2", "pytest>=9.1.1", "twine>=6.2.0", "wheel>=0.47.0"]
